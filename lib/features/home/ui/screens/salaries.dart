import 'package:flutter/material.dart';

class SalariesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Salaries'),
      ),
      body: Center(
        child: Text('Salaries Page Content'),
      ),
    );
  }
}
